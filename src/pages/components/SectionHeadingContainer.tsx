const SectionHeadingContainer = ({ children }: { children: React.ReactElement }) => {
    return (
        <div className="mb-4 lg:mb-8">{children}</div>
    )
}

export default SectionHeadingContainer